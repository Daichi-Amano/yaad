name: Deploy to Vercel

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Hobbyプランでは、第三者のコミットでもデプロイをトリガーするために必要
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Deploy to Vercel
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
        run: |
          if [ -z "$VERCEL_TOKEN" ]; then
            echo "❌ VERCEL_TOKEN が設定されていません。"
            echo "GitHubリポジトリの Settings → Secrets and variables → Actions で設定してください。"
            exit 1
          fi

          echo "🚀 Vercelへのデプロイを開始しています..."

          # Git authorを設定して、Vercelのチームアクセスチェックを回避
          # これにより、別ユーザーのpushでもデプロイが可能になります
          git config user.email "amano@andpro.jp"
          git config user.name "daichi-amano"

          # Vercel CLIでデプロイ（--scopeを明示的に指定）
          if [ -n "$VERCEL_ORG_ID" ]; then
            vercel --prod --token="$VERCEL_TOKEN" --scope="$VERCEL_ORG_ID" --yes
          else
            vercel --prod --token="$VERCEL_TOKEN" --yes
          fi

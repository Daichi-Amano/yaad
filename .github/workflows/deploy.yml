name: Deploy to Vercel

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to Vercel
        env:
          VERCEL_DEPLOY_HOOK_URL: ${{ secrets.VERCEL_DEPLOY_HOOK_URL }}
        run: |
          if [ -z "$VERCEL_DEPLOY_HOOK_URL" ]; then
            echo "❌ VERCEL_DEPLOY_HOOK_URL が設定されていません。"
            exit 1
          fi

          echo "🚀 Deploy Hookをトリガーしています..."
          echo "📍 タイムスタンプ: $(date -u +"%Y-%m-%d %H:%M:%S UTC")"
          echo "📍 ブランチ: ${{ github.ref_name }}"
          echo "📍 コミット: ${{ github.sha }}"
          echo "📍 コミットメッセージ: ${{ github.event.head_commit.message || 'N/A' }}"
          echo "📍 コミット作成者: ${{ github.event.head_commit.author.name || github.actor }} <${{ github.event.head_commit.author.email || 'N/A' }}>"

          response=$(curl -s -w "\n%{http_code}" -X POST "$VERCEL_DEPLOY_HOOK_URL")
          http_code=$(echo "$response" | tail -1)
          body=$(echo "$response" | sed '$d')

          echo ""
          echo "📋 Deploy Hook レスポンス:"
          echo "   HTTPステータス: $http_code"
          echo "   レスポンスボディ: $body"

          if [ "$http_code" = "200" ] || [ "$http_code" = "201" ]; then
            echo ""
            echo "✅ デプロイが正常にトリガーされました。"
            
            # レスポンスからJob IDを抽出（もしあれば）
            if echo "$body" | grep -q '"id"'; then
              job_id=$(echo "$body" | grep -o '"id":"[^"]*"' | head -1 | cut -d'"' -f4)
              echo "   Job ID: $job_id"
            fi
          else
            echo ""
            echo "❌ デプロイのトリガーに失敗しました (HTTP $http_code)"
            exit 1
          fi

name: Deploy to Vercel

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Hobbyプランでは、第三者のコミットでもデプロイをトリガーするために必要
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Deploy to Vercel
        env:
          VERCEL_DEPLOY_HOOK_URL: ${{ secrets.VERCEL_DEPLOY_HOOK_URL }}
        run: |
          if [ -z "$VERCEL_DEPLOY_HOOK_URL" ]; then
            echo "❌ VERCEL_DEPLOY_HOOK_URL が設定されていません。"
            echo "Vercelのダッシュボードで Deploy Hook を作成し、"
            echo "GitHubリポジトリの Settings → Secrets and variables → Actions で設定してください。"
            echo ""
            echo "Deploy Hook の作成方法:"
            echo "1. Vercelダッシュボード → プロジェクト設定 → Git → Deploy Hooks"
            echo "2. 新しいHookを作成（ブランチ: master）"
            echo "3. 生成されたURLを VERCEL_DEPLOY_HOOK_URL として設定"
            exit 1
          fi

          echo "🚀 Vercelへのデプロイを開始しています..."

          # Deploy Hookを使用することで、Git authorのチェックを回避できます
          # これにより、別ユーザーのpushでもデプロイが可能になります
          response=$(curl -X POST "$VERCEL_DEPLOY_HOOK_URL")

          if [ $? -eq 0 ]; then
            echo "✅ デプロイが正常にトリガーされました。"
            echo "$response"
          else
            echo "❌ デプロイのトリガーに失敗しました。"
            exit 1
          fi
